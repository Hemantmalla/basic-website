# basic-website
A client-side web application built with HTML, CSS, and jQuery that demonstrates asynchronous data handling. It fetches user records from the JSONPlaceholder API and renders them in a responsive table. Features include delegated event handling, dynamic hover effects.
